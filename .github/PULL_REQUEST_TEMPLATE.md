> Generated with AI assistance (OpenCode).

## What
> One to three sentences describing the change.

## Why
> Ticket implemented. Closes #<n>

## How
> Approach taken. Reminder: commits should read as a story (one logical change per commit: `test:` red → `feat:`/`fix:` green → optional `refactor:`).

## Testing
> What was run and the evidence (TDD: red → green at the agreed seams).

## Screenshots
> Optional for UI changes. Remove hint if not applicable.

## Checklist
- [ ] Diff is ≤300 lines (code + tests) — if larger, this PR should be split
- [ ] One ticket, one branch, one PR
- [ ] Commits are atomic (one logical change each)
- [ ] `/code-review` blockers cleared
- [ ] `/security-review` run (slice touches auth, secrets, input, or data) or N/A stated
- [ ] CI green (or N/A — docs-only change)