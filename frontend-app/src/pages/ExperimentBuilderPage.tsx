import { PlaceholderPage } from "./PlaceholderPage";

export function ExperimentBuilderPage() {
  return (
    <PlaceholderPage
      title="Experiment Builder"
      description="Define participants (Roles, Personas, Agents, Shells, seed Stances) and topology & run settings for an Experiment."
    />
  );
}
