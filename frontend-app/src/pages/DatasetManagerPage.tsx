import { PlaceholderPage } from "./PlaceholderPage";

export function DatasetManagerPage() {
  return (
    <PlaceholderPage
      title="Dataset Manager"
      description="Map the seed Dataset into Context Shells, observe the live Intermediate Dataset, and view or export the Final Dataset as JSON/CSV."
    />
  );
}
